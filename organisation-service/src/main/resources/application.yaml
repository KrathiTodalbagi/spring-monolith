#server: The commented properties are taken from Config server
#  port: 8083
spring:
  application:
    name: organisation-service
  config:
    import: optional:configserver:http://localhost:8888
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        missing-queues-fatal: false
  cloud:
#    open-feign:
#      circuitbreaker:
#        enabled: true
    bus:
      enabled: true
      destination: springCloudBus
    stream:
      bindings:
        springCloudBusInput:
          group: ${spring.application.name}
  datasource:
#    url: 'jdbc:postgresql://localhost:5432/organisation_db'
#    username: 'postgres'
    password: '0211'
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: 'org.hibernate.dialect.PostgreSQLDialect'

#eureka:
#  instance:
#    client:
#      serverUrl:
#        defaultZone: 'http://localhost:8761/eureka/'
